import{_ as s,o as i,c as t,ag as e}from"./chunks/framework.BZohXCq9.js";const c=JSON.parse('{"title":"RK3588 NPU 输入踩坑复盘：能跑不等于接对","description":"复盘 RKNN 输入适配中的真实问题：FP16 类型混淆、DMA-BUF 后 CPU 仍高、重复物化、layout 漂移与 zero-copy 假象","frontmatter":{"title":"RK3588 NPU 输入踩坑复盘：能跑不等于接对","description":"复盘 RKNN 输入适配中的真实问题：FP16 类型混淆、DMA-BUF 后 CPU 仍高、重复物化、layout 漂移与 zero-copy 假象","tags":["RK3588","RKNN","NPU","DMA-BUF","OpenCL","问题复盘"],"date":"2026/07/20"},"headers":[],"relativePath":"linux/rknn-npu-input-pipeline.md","filePath":"linux/rknn-npu-input-pipeline.md","lastUpdated":1784519165000}'),n={name:"linux/rknn-npu-input-pipeline.md"};function p(l,a,h,r,d,o){return i(),t("div",null,[...a[0]||(a[0]=[e(`<h1 id="rk3588-npu-输入踩坑复盘-能跑不等于接对" tabindex="-1">RK3588 NPU 输入踩坑复盘：能跑不等于接对 <a class="header-anchor" href="#rk3588-npu-输入踩坑复盘-能跑不等于接对" aria-label="Permalink to &quot;RK3588 NPU 输入踩坑复盘：能跑不等于接对&quot;">​</a></h1><p>这篇文章不从理想架构开始，而从几次真实误判开始。因为 NPU 输入最麻烦的地方不是 API 不会调用，而是代码能运行、结果也能出来，却仍然可能存在类型混淆、隐式复制、布局漂移和伪 zero-copy。</p><p>把一张图的地址交给 RKNN，并不等于完成了 NPU 输入。真正的输入是一份完整合同：图像表达什么、tensor 怎样排列、数值如何变换、buffer 怎样对齐，以及 CPU、RGA、GPU 与 NPU 在什么时刻拥有它。</p><h2 id="先看问题-四次最有价值的误判" tabindex="-1">先看问题：四次最有价值的误判 <a class="header-anchor" href="#先看问题-四次最有价值的误判" aria-label="Permalink to &quot;先看问题：四次最有价值的误判&quot;">​</a></h2><table tabindex="0"><thead><tr><th>现场问题</th><th>第一反应</th><th>最终定位</th></tr></thead><tbody><tr><td>FP16 模型到底该传 FP32 还是 FP16</td><td>根据模型名称直接选 dtype</td><td>模型精度、Runtime 输入和 NPU 计算精度是三份合同</td></tr><tr><td>相机已经输出 DMA-BUF，CPU 却没有明显下降</td><td>继续优化 NPU 推理</td><td>CPU 仍耗在 ROS、颜色转换、float32 物化与输入复制</td></tr><tr><td>同时输出 atlas 和四对独立图像</td><td>多一种输出更方便</td><td>把逻辑视图做成了重复的物理存储</td></tr><tr><td>下游能按固定偏移切 ROI</td><td>常量一致就够了</td><td>layout 没进入 typed contract，组件升级后必然漂移</td></tr></tbody></table><h2 id="踩坑一-把-模型是-fp16-理解成-应用必须传-fp16" tabindex="-1">踩坑一：把“模型是 FP16”理解成“应用必须传 FP16” <a class="header-anchor" href="#踩坑一-把-模型是-fp16-理解成-应用必须传-fp16" aria-label="Permalink to &quot;踩坑一：把“模型是 FP16”理解成“应用必须传 FP16”&quot;">​</a></h2><h3 id="现场症状" tabindex="-1">现场症状 <a class="header-anchor" href="#现场症状" aria-label="Permalink to &quot;现场症状&quot;">​</a></h3><p>一个非量化模型已经能通过 <code>rknn_inputs_set()</code> 正常推理，上游使用的是 float32 buffer。准备切换高性能输入时，问题变成了：“模型是 FP16，那应用是不是必须直接生产 FP16？”</p><p>这类问题表面在选 dtype，实际已经把三个不同概念混到了一起：模型图的精度、Runtime API 接受的输入，以及 NPU 内部最终执行的精度。</p><h3 id="错误判断" tabindex="-1">错误判断 <a class="header-anchor" href="#错误判断" aria-label="Permalink to &quot;错误判断&quot;">​</a></h3><p>第一种误判是看到 FP16 模型就把应用 buffer 改成 FP16。第二种误判则相反：既然 FP32 输入能跑，就认为整个网络仍按 FP32 计算。</p><p>两者都可能让程序继续运行，所以单看返回码找不到问题。</p><h3 id="证据链" tabindex="-1">证据链 <a class="header-anchor" href="#证据链" aria-label="Permalink to &quot;证据链&quot;">​</a></h3><p>真正有效的证据不是模型文件名，而是 <code>RKNN_QUERY_INPUT_ATTR</code> 返回的 <code>type</code>、<code>fmt</code>、<code>dims</code>、量化信息和 <code>size_with_stride</code>。同时还要检查调用路径是否启用了 Runtime 转换。</p><p><code>pass_through = 0</code> 时，调用方声明的 dtype/layout 可以先由 Runtime 转换；直接绑定输入内存时，数据则应更严格地匹配模型输入合同。</p><h3 id="根因与解决" tabindex="-1">根因与解决 <a class="header-anchor" href="#根因与解决" aria-label="Permalink to &quot;根因与解决&quot;">​</a></h3><p>根因是把“模型精度”当成了“应用输入格式”。修复时没有直接跳到 zero-copy，而是保留普通 FP32 输入作为正确性基线，再查询当前 <code>.rknn</code> 的真实属性，最后决定 OpenCL 应输出 FP16 native tensor，还是继续让 Runtime 转换。</p><h3 id="怎么验证修复" tabindex="-1">怎么验证修复 <a class="header-anchor" href="#怎么验证修复" aria-label="Permalink to &quot;怎么验证修复&quot;">​</a></h3><p>同一输入至少比较三份结果：训练框架或参考实现、普通 Runtime 输入、native/zero-copy 输入。只有输出误差在预期范围内，并且预处理与提交耗时确实下降，dtype 改造才算完成。</p><h2 id="踩坑二-换成-dma-buf-后-cpu-为什么还是高" tabindex="-1">踩坑二：换成 DMA-BUF 后，CPU 为什么还是高 <a class="header-anchor" href="#踩坑二-换成-dma-buf-后-cpu-为什么还是高" aria-label="Permalink to &quot;踩坑二：换成 DMA-BUF 后，CPU 为什么还是高&quot;">​</a></h2><h3 id="现场症状-1" tabindex="-1">现场症状 <a class="header-anchor" href="#现场症状-1" aria-label="Permalink to &quot;现场症状&quot;">​</a></h3><p>相机流水线已经能输出 DMA-BUF，于是很容易认为下游模型“把 topic 换成 DMA-BUF”就完成了优化。但检查消费端热路径后，仍然能看到下面这条链：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ROS Image exact-time sync</span></span>
<span class="line"><span>  → cv_bridge</span></span>
<span class="line"><span>  → BGR to RGB</span></span>
<span class="line"><span>  → convertTo(float32)</span></span>
<span class="line"><span>  → 独立 float buffer</span></span>
<span class="line"><span>  → rknn_inputs_set()</span></span></code></pre></div><p>数据入口看起来更现代了，真正吃 CPU 的预处理和输入复制却一个都没消失。</p><h3 id="错误判断-1" tabindex="-1">错误判断 <a class="header-anchor" href="#错误判断-1" aria-label="Permalink to &quot;错误判断&quot;">​</a></h3><p>错误在于把传输载体当成完整数据路径。DMA-BUF 只解决“这块内存能否跨组件共享”，不会自动消除 <code>cv::Mat</code> 物化、颜色转换、float32 扩展和 Runtime 内部转换。</p><h3 id="证据链-1" tabindex="-1">证据链 <a class="header-anchor" href="#证据链-1" aria-label="Permalink to &quot;证据链&quot;">​</a></h3><p>这类问题不能只看 NPU 利用率。需要同时做两件事：沿源码逐段标出每帧的 map、copy、convert 和 submit；把预处理、输入提交、NPU 推理、后处理分别计时。</p><p>如果 NPU 推理时间基本不变，而 CPU 仍耗在 <code>cv_bridge</code>、<code>cvtColor</code>、<code>convertTo</code> 和 <code>rknn_inputs_set()</code>，继续调 NPU core mask 并不能解决问题。</p><h3 id="根因与解决-1" tabindex="-1">根因与解决 <a class="header-anchor" href="#根因与解决-1" aria-label="Permalink to &quot;根因与解决&quot;">​</a></h3><p>根因是只替换了接口，没有替换数据面。正确方向是让模型成为相机图的直接 consumer，由 RGA/OpenCL 完成 resize、颜色和 native tensor 生成，再用 RKNN IO memory 接口提交，ROS 图像桥只保留为兼容或诊断输出。</p><p>需要特别说明：这次结论来自代码热路径与架构分析，它证明“仅换 topic 不够”，但不能替代改造后的板端 A/B 数据。完成实现后仍要重新测 CPU、RSS、带宽、帧率与 NPU 利用率。</p><h2 id="踩坑三-同时输出-atlas-和四对图像-资源反而更差" tabindex="-1">踩坑三：同时输出 atlas 和四对图像，资源反而更差 <a class="header-anchor" href="#踩坑三-同时输出-atlas-和四对图像-资源反而更差" aria-label="Permalink to &quot;踩坑三：同时输出 atlas 和四对图像，资源反而更差&quot;">​</a></h2><h3 id="现场症状-2" tabindex="-1">现场症状 <a class="header-anchor" href="#现场症状-2" aria-label="Permalink to &quot;现场症状&quot;">​</a></h3><p>为了同时服务不同 consumer，一个看似方便的方案是既保留两张 <code>320×1024</code> 双目 atlas，又额外生成四对 <code>320×256</code> 图像。接口确实更直观，但同一批像素被物化了两次。</p><h3 id="错误判断-2" tabindex="-1">错误判断 <a class="header-anchor" href="#错误判断-2" aria-label="Permalink to &quot;错误判断&quot;">​</a></h3><p>这里混淆了逻辑视图和物理存储。四对图像只是 atlas 中四个纵向 ROI，不需要天然对应八块新的像素内存。</p><h3 id="证据链-2" tabindex="-1">证据链 <a class="header-anchor" href="#证据链-2" aria-label="Permalink to &quot;证据链&quot;">​</a></h3><p>判断方法很直接：把每帧新增的 buffer 数量、单帧字节数、队列深度和帧率相乘，再检查额外的颜色转换与复制次数。即使单帧看起来不大，连续流中的内存带宽和 CMA/DMA-BUF 压力也会被放大。</p><h3 id="根因与解决-2" tabindex="-1">根因与解决 <a class="header-anchor" href="#根因与解决-2" aria-label="Permalink to &quot;根因与解决&quot;">​</a></h3><p>根因是为了 consumer 方便，破坏了单一物理表示。最终方案只保留左右两张 atlas DMA-BUF，并用 typed ROI metadata 表达四对逻辑切片：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pair 0: y =   0, height = 256</span></span>
<span class="line"><span>pair 1: y = 256, height = 256</span></span>
<span class="line"><span>pair 2: y = 512, height = 256</span></span>
<span class="line"><span>pair 3: y = 768, height = 256</span></span></code></pre></div><p>consumer 读取 ROI view，不重新生成独立图像。验证时要确认四个区域边界不越界、时间戳继承一致，并比较 ROI view 与旧物化图像的像素结果。</p><h2 id="踩坑四-让下游自己猜-layout-测试迟早会失败" tabindex="-1">踩坑四：让下游自己猜 layout，测试迟早会失败 <a class="header-anchor" href="#踩坑四-让下游自己猜-layout-测试迟早会失败" aria-label="Permalink to &quot;踩坑四：让下游自己猜 layout，测试迟早会失败&quot;">​</a></h2><h3 id="现场症状-3" tabindex="-1">现场症状 <a class="header-anchor" href="#现场症状-3" aria-label="Permalink to &quot;现场症状&quot;">​</a></h3><p>早期实现里，atlas 的宽高和四段 ROI 偏移散落在消费端常量中。第一次写 contract test 时，测试直接假设执行器已经返回 layout；实际接口里根本没有这个字段。</p><p>这个失败反而暴露了真正的问题：layout 只存在于人的共识和几个常量里，没有进入执行结果合同。</p><h3 id="错误判断-3" tabindex="-1">错误判断 <a class="header-anchor" href="#错误判断-3" aria-label="Permalink to &quot;错误判断&quot;">​</a></h3><p>最初想法是让下游根据固定尺寸重新计算 ROI。短期能工作，但一旦分辨率、排列方式或 stride 变化，不同 consumer 会各自得到一份“看起来合理”的解释。</p><h3 id="根因与解决-3" tabindex="-1">根因与解决 <a class="header-anchor" href="#根因与解决-3" aria-label="Permalink to &quot;根因与解决&quot;">​</a></h3><p>根因不是测试写早了，而是 contract 放错了层。修复后，由实际生成 atlas 的 executor/builder 输出 typed layout；中间 element 只验证并转发；算法 consumer 只消费，不再发明布局。</p><h3 id="怎么验证修复-1" tabindex="-1">怎么验证修复 <a class="header-anchor" href="#怎么验证修复-1" aria-label="Permalink to &quot;怎么验证修复&quot;">​</a></h3><p>验证分成两组：一组检查 canonical atlas 与四个 ROI 的几何关系，另一组检查硬件执行结果确实携带同一份 layout。两组包级测试全部通过后，才算关闭这个问题。</p><p>这次修复带来的价值不只是“测试绿了”。它消除了相机侧、OpenCL 侧和 NPU consumer 之间最容易静默漂移的一份隐式协议。</p><h2 id="先把输入拆成三层合同" tabindex="-1">先把输入拆成三层合同 <a class="header-anchor" href="#先把输入拆成三层合同" aria-label="Permalink to &quot;先把输入拆成三层合同&quot;">​</a></h2><h3 id="_1-图像语义合同" tabindex="-1">1. 图像语义合同 <a class="header-anchor" href="#_1-图像语义合同" aria-label="Permalink to &quot;1. 图像语义合同&quot;">​</a></h3><p>相机通常给出 NV12、NV21、YUYV 或 Bayer 数据，模型训练时看到的却可能是 RGB 图片。二者之间至少要明确：</p><ul><li>输入来自完整图像还是 ROI；</li><li>resize 是直接拉伸、等比例缩放还是 letterbox；</li><li>YUV 使用哪种色彩矩阵和数值范围；</li><li>输出是 RGB 还是 BGR；</li><li>是否需要减均值、除标准差或缩放到特定区间。</li></ul><p>这些信息不会因为 buffer 能被 NPU 访问就自动正确。最危险的情况不是接口报错，而是图像“看起来差不多”，模型精度却悄悄下降。</p><h3 id="_2-tensor-合同" tabindex="-1">2. Tensor 合同 <a class="header-anchor" href="#_2-tensor-合同" aria-label="Permalink to &quot;2. Tensor 合同&quot;">​</a></h3><p>图像变成 tensor 后，还需要一组可执行的属性：</p><table tabindex="0"><thead><tr><th>属性</th><th>需要回答的问题</th></tr></thead><tbody><tr><td>shape</td><td>batch、channel、height、width 各是多少</td></tr><tr><td>layout</td><td>数据按 NCHW 还是 NHWC 排列</td></tr><tr><td>dtype</td><td>应用提交 FP32、FP16、INT8 还是 UINT8</td></tr><tr><td>quantization</td><td>scale、zero point 和量化方式是什么</td></tr><tr><td>stride</td><td>每行和整个 buffer 的实际跨度是多少</td></tr></tbody></table><p>这里最容易混淆的是模型精度和 Runtime 输入类型。FP16 模型不代表应用只能提交 FP16；普通输入路径可以发生类型转换。但一旦追求低拷贝和稳定延迟，上游就应该尽量直接生产模型需要的 native contract。</p><h3 id="_3-内存与所有权合同" tabindex="-1">3. 内存与所有权合同 <a class="header-anchor" href="#_3-内存与所有权合同" aria-label="Permalink to &quot;3. 内存与所有权合同&quot;">​</a></h3><p>即使 tensor 属性全部正确，内存仍有自己的规则：谁分配、谁写入、何时交给 NPU、什么时候允许复用、CPU 与设备之间是否需要同步缓存。</p><p>所以，“zero-copy”不能只看有没有 DMA-BUF fd。应用中间做了一次 <code>memcpy</code>，或者 Runtime 又做了 layout/dtype 转换，端到端链路就仍然存在搬运。</p><h2 id="运行时第一步-查询-不要猜" tabindex="-1">运行时第一步：查询，不要猜 <a class="header-anchor" href="#运行时第一步-查询-不要猜" aria-label="Permalink to &quot;运行时第一步：查询，不要猜&quot;">​</a></h2><p>加载 <code>.rknn</code> 后，应先把每个输入的真实属性打印出来：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rknn_input_output_num io_num{};</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rknn_query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, RKNN_QUERY_IN_OUT_NUM, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">io_num, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(io_num));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">uint32_t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> io_num.n_input; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">i) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    rknn_tensor_attr attr{};</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    attr.index </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ret </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> rknn_query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ctx, RKNN_QUERY_INPUT_ATTR, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">attr, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(attr));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (ret </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RKNN_SUCC) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        throw</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> std</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">runtime_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;query RKNN input attr failed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    printf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;input[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">] name=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> type=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fmt=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> size=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">           &quot;w_stride=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> size_with_stride=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%u</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">           &quot;qnt=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> zp=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scale=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">%f\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">           i, attr.name, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get_type_string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(attr.type),</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">           get_format_string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(attr.fmt), attr.size,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">           attr.w_stride, attr.size_with_stride,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">           get_qnt_type_string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(attr.qnt_type), attr.zp, attr.scale);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>这份 dump 应与模型 hash 一起进入部署记录。模型文件一旦替换，输入合同也必须重新验证，而不是沿用旧代码里的宽高和类型常量。</p><h2 id="一条可维护的-npu-输入流水线" tabindex="-1">一条可维护的 NPU 输入流水线 <a class="header-anchor" href="#一条可维护的-npu-输入流水线" aria-label="Permalink to &quot;一条可维护的 NPU 输入流水线&quot;">​</a></h2><p>对于 RK3588 视觉应用，可以把数据流明确成以下阶段：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>V4L2 / Rockit capture</span></span>
<span class="line"><span>  → NV12 DMA-BUF</span></span>
<span class="line"><span>  → crop / resize / letterbox</span></span>
<span class="line"><span>  → YUV to RGB/BGR</span></span>
<span class="line"><span>  → normalize or quantize</span></span>
<span class="line"><span>  → native dtype + layout + stride</span></span>
<span class="line"><span>  → RKNN input memory</span></span>
<span class="line"><span>  → cache/device synchronization</span></span>
<span class="line"><span>  → rknn_run()</span></span></code></pre></div><p>这条链路最重要的设计原则是：每个阶段只负责一种变换，并把输出属性写成明确合同。不要让消费端根据 buffer 大小反推 layout，也不要让多个组件分别做一次颜色或归一化转换。</p><h3 id="rga-适合做什么" tabindex="-1">RGA 适合做什么 <a class="header-anchor" href="#rga-适合做什么" aria-label="Permalink to &quot;RGA 适合做什么&quot;">​</a></h3><p>RGA 适合处理 crop、resize、letterbox 和常见像素格式转换。官方示例也展示了用 RGA 将图像缩放到 RKNN 创建的输入内存。</p><p>但“RGA 已经把图送进输入 buffer”仍不代表 tensor 一定正确。RGB/BGR 顺序、归一化、量化、layout 和 stride 仍需要按模型合同确认。</p><h3 id="opencl-适合做什么" tabindex="-1">OpenCL 适合做什么 <a class="header-anchor" href="#opencl-适合做什么" aria-label="Permalink to &quot;OpenCL 适合做什么&quot;">​</a></h3><p>当预处理需要自定义畸变校正、atlas/ROI 重排、归一化或 FP16 tensor 生成时，OpenCL 更适合表达融合算子。理想状态是让 OpenCL 直接写入最终输入 buffer，避免先生成中间 <code>cv::Mat</code>，再由 CPU 转成 float tensor。</p><p>这里应该优化的是完整的数据流，而不是单个 kernel 的耗时。减少一次 CPU 物化、一次颜色转换或一次大块复制，通常比继续压缩已经很短的算子更有价值。</p><h2 id="普通输入与-zero-copy-是两种验收阶段" tabindex="-1">普通输入与 zero-copy 是两种验收阶段 <a class="header-anchor" href="#普通输入与-zero-copy-是两种验收阶段" aria-label="Permalink to &quot;普通输入与 zero-copy 是两种验收阶段&quot;">​</a></h2><h3 id="普通路径-先证明语义正确" tabindex="-1">普通路径：先证明语义正确 <a class="header-anchor" href="#普通路径-先证明语义正确" aria-label="Permalink to &quot;普通路径：先证明语义正确&quot;">​</a></h3><p><code>rknn_inputs_set()</code> 适合模型接入初期。<code>pass_through = 0</code> 时，Runtime 会依据调用方填写的 <code>type</code> 与 <code>fmt</code> 做必要转换：</p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">rknn_input input{};</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.index </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.buf </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rgb_data;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> rgb_bytes;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.type </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RKNN_TENSOR_UINT8;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.fmt </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> RKNN_TENSOR_NHWC;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input.pass_through </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rknn_inputs_set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">input);</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rknn_run</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nullptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>这条路径的价值是快速建立正确性基线。它不应该被默认当成最终性能方案，因为 Runtime 内部可能仍有 dtype 或 layout 转换。</p><h3 id="zero-copy-路径-再消除搬运和隐式转换" tabindex="-1">Zero-copy 路径：再消除搬运和隐式转换 <a class="header-anchor" href="#zero-copy-路径-再消除搬运和隐式转换" aria-label="Permalink to &quot;Zero-copy 路径：再消除搬运和隐式转换&quot;">​</a></h3><p>稳定生产路径可以使用 <code>rknn_create_mem()</code> 创建输入内存，或按具体 SDK 能力导入外部分配的 fd，然后通过 <code>rknn_set_io_mem()</code> 绑定 tensor。</p><p>这时必须同时满足四个条件：</p><ol><li>buffer 容量覆盖 <code>size_with_stride</code>；</li><li>写入内容与声明的 dtype/layout 一致；</li><li>producer 完成写入后才把内存交给 NPU；</li><li>cacheable 内存在 CPU 与设备交接时执行正确的同步。</li></ol><p><code>pass_through</code> 也不是一个“性能开关”。打开它意味着数据直接进入模型输入节点，不再由 Runtime 按 <code>type</code> 和 <code>fmt</code> 转换。合同有任何一项不匹配，结果都可能错误。</p><h2 id="fp16-模型的输入到底该怎么做" tabindex="-1">FP16 模型的输入到底该怎么做 <a class="header-anchor" href="#fp16-模型的输入到底该怎么做" aria-label="Permalink to &quot;FP16 模型的输入到底该怎么做&quot;">​</a></h2><p>如果 <code>.rknn</code> 是非量化 FP16 模型，可以按两阶段推进：</p><p>第一阶段先用 FP32 或 Runtime 明确支持的输入类型跑通参考结果，证明 resize、颜色、归一化和输出解析全部正确。第二阶段再根据 <code>RKNN_QUERY_INPUT_ATTR</code> 的结果，把上游输出收敛到模型 native dtype/layout，并测量 CPU、内存带宽和端到端延迟是否真的下降。</p><p>不要因为模型叫“FP16”就跳过查询，也不要为了 zero-copy 把 UINT8 图像强行解释成 FP16 tensor。类型匹配只是合同的一部分，数值范围同样必须匹配训练和转换配置。</p><h2 id="int8-uint8-输入最容易错在哪里" tabindex="-1">INT8/UINT8 输入最容易错在哪里 <a class="header-anchor" href="#int8-uint8-输入最容易错在哪里" aria-label="Permalink to &quot;INT8/UINT8 输入最容易错在哪里&quot;">​</a></h2><p>量化模型需要重点确认 <code>qnt_type</code>、<code>scale</code> 与 <code>zp</code>。以非对称量化为例，浮点值与量化值之间通常可以写成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>q = round(real_value / scale) + zero_point</span></span>
<span class="line"><span>real_value ≈ (q - zero_point) × scale</span></span></code></pre></div><p>但是否由应用执行这一步，取决于模型与输入路径。如果 Toolkit 已把预处理固化进模型，而应用又手动量化一次，结果就会发生双重量化。反过来，如果 <code>pass_through</code> 直接提交 native INT8，而上游没有完成模型要求的量化，数值也会完全错误。</p><p>所以不能从 <code>INT8</code> 或 <code>UINT8</code> 这一个枚举推导完整处理方式。必须同时检查转换配置、tensor attr 与实际调用路径。</p><h2 id="用症状快速定位输入问题" tabindex="-1">用症状快速定位输入问题 <a class="header-anchor" href="#用症状快速定位输入问题" aria-label="Permalink to &quot;用症状快速定位输入问题&quot;">​</a></h2><table tabindex="0"><thead><tr><th>症状</th><th>优先检查</th></tr></thead><tbody><tr><td>能运行但分类/检测结果明显不对</td><td>RGB/BGR、normalize、量化是否重复</td></tr><tr><td>画面主体位置对，框整体偏移</td><td>resize、letterbox、ROI 与反变换</td></tr><tr><td>普通路径正确，zero-copy 错误</td><td>layout、stride、buffer 容量、cache sync</td></tr><tr><td>换模型后随机崩溃或结果漂移</td><td>输入数量、shape、<code>size_with_stride</code></td></tr><tr><td>CPU 仍然很高</td><td><code>memcpy</code>、<code>cv::Mat</code>、Runtime 隐式转换</td></tr><tr><td>首帧正确，后续帧偶发污染</td><td>buffer 生命周期、fence/同步与复用时机</td></tr></tbody></table><h2 id="最小验收闭环" tabindex="-1">最小验收闭环 <a class="header-anchor" href="#最小验收闭环" aria-label="Permalink to &quot;最小验收闭环&quot;">​</a></h2><p>我更倾向把 NPU 输入验收拆成五步：</p><ol><li>用同一张输入图对齐训练框架、Toolkit 模拟器与板端输出；</li><li>保存预处理后的 tensor，逐元素或按统计量比较；</li><li>比较普通输入和 zero-copy 输入的结果误差；</li><li>分开统计预处理、提交、推理和后处理耗时；</li><li>在连续流中验证 buffer 复用、缓存同步与长时间稳定性。</li></ol><p>只有结果一致、链路没有隐式复制、buffer 所有权可证明，才能说 NPU 输入真正接好了。</p><h2 id="结语" tabindex="-1">结语 <a class="header-anchor" href="#结语" aria-label="Permalink to &quot;结语&quot;">​</a></h2><p>NPU 输入设计的核心不是选择 FP16 还是 INT8，而是建立一份不会被误解的端到端合同。图像语义、tensor 属性和内存所有权缺一不可。</p><p>当这份合同足够明确，CPU 基线路径、RGA/OpenCL 预处理和 RKNN zero-copy 才能逐步切换、分别验证。否则，“零拷贝”很容易只剩一个 DMA-BUF fd，而错误和性能损耗被藏在下一层。</p><h2 id="延伸阅读" tabindex="-1">延伸阅读 <a class="header-anchor" href="#延伸阅读" aria-label="Permalink to &quot;延伸阅读&quot;">​</a></h2><ul><li><a href="/linux/rknn-input-dtype">RKNN 输入到底是 FP32、FP16 还是 INT8</a></li><li><a href="/linux/rk3588-camera-pipeline">RK3588 高性能相机流水线</a></li><li><a href="/linux/dmabuf-ownership-contract">跨进程 DMA-BUF 所有权合同</a></li><li><a href="https://github.com/airockchip/rknn-toolkit2" target="_blank" rel="noreferrer">RKNN-Toolkit2 官方仓库</a></li><li><a href="https://github.com/airockchip/rknn-toolkit2/tree/master/rknpu2/examples/rknn_zero_copy" target="_blank" rel="noreferrer">官方 zero-copy 示例说明</a></li><li><a href="https://github.com/airockchip/rknn-toolkit2/blob/master/rknpu2/examples/rknn_api_demo/src/rknn_create_mem_with_rga_demo.cpp" target="_blank" rel="noreferrer">官方 RGA + RKNN 输入示例</a></li></ul>`,110)])])}const u=s(n,[["render",p]]);export{c as __pageData,u as default};
